# tracking_training_client

This is your Serverpod client. The code in here is mostly generated by
Serverpod, but you may want to make changes if you are adding modules to your
project.

## Notes

1. Treat `lib/src/protocol/` as generated output.
2. When server endpoints, protocol models, or serializable types change, run `serverpod generate` from `tracking_training_server/` to refresh this package.
3. Keep this package in sync with both `tracking_training_server/` and `tracking_training_flutter/` when API contracts change.
